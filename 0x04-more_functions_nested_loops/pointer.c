#include <stdio.h>
int main(void)
{
	const int MAX =3;
	
	int *p;/*pointer declaration*/
	int var = 20; /*actual variable definition*/
	p= &var; 
	int *x =NULL;
	printf(" Value of var is %d :::: address is %p\n", var,p);
	printf(" Value of *d is %p\n",x);
	int w , *a;
	int val[] ={10,200,300};
	  /* let us have array address in pointer */
	a = val;
	
	for (i=0;i<Max;i++)
	{
		printf("");
	}
}
