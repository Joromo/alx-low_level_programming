#include "main.h"
/**
* mul- a function that multiplies two integers.
* @a : interger to be mutliplied
* @b : interger to be mutliplied
* Return:0 if succesfull
*/
int mul(int a, int b)
{
	return (a * b);
	return (0);
}
