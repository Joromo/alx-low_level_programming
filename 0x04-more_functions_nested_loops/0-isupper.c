#include "main.h"
/**
* _isupper - Prints upper case letters
* @c: the character to check
* Return: i if Upper 0 is otherwise
*/
int _isupper(int c)
{
	if (c == 'A' && c < 'Z')
	{
		return (1);
	}
	return (0);
}
